# docs/

Screenshots referenced by the top-level `README.md`. Add PNGs here:

- `chat.png` — a cited answer with the **Sources** panel (audio seeked to the timestamp)
- `figure.png` — an answer that renders the referenced slide **figure inline**
- `course-map.png` — the **Course Map** concept graph
- `eval.png` — the eval table (grader ablation)
